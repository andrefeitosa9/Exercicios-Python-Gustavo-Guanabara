# Exercício Python 50: Desenvolva um programa que leia seis números inteiros e mostre a soma apenas daqueles que forem pares.
# Se o valor digitado for ímpar, desconsidere-o.
print ('Essa é uma calculadora que soma 6 números, contanto que sejam pares')
soma = 0

for c in range (1,7):
    num = int(input(f'Digite o {c}º valor: '))
    if num % 2 == 0:
        soma += num
print (f' A soma dos números pares dos 6 números digitados é igual a {soma}')
