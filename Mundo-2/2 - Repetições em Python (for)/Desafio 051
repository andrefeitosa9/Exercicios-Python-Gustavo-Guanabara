# Exercício Python 51: Desenvolva um programa que leia o primeiro termo e a razão de uma PA.
# No final, mostre os 10 primeiros termos dessa progressão.

print ('Calculadora de progressão aritmética \n')

primeiro_termo = int(input('Digite o primeiro termo da PA: '))
razao = int(input('Digite a razão da PA: '))
decimo = primeiro_termo + (10) * razao
for pa in range (primeiro_termo, decimo, razao):
    print (f'{pa}', end =' -> ')
print ("FIM")
