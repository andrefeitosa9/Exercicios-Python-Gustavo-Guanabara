# Exercício Python 48: Faça um programa que calcule a soma entre todos os números que são múltiplos
# de três e que são ímpares e que se encontram no intervalo de 1 até 500.

s = 0

print ('Soma dos múltiplos de três, além de ímpares, entre 1 e 500 \n')

for c in range (1,501):
    if c % 3 == 0:
        if c % 2 != 0:
            s += c
print (f' A soma dos mútiplos de três que são ímpares entre 1 e 500 foi : {s}')
